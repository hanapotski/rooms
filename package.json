{
  "name": "twine-mystery-starter",
  "version": "1.0.0",
  "private": true,
  "description": "Build and deploy workflow for the Quiet Floor Twine game",
  "scripts": {
    "setup": "bash scripts/install-tweego.sh",
    "build": "bash scripts/build.sh",
    "preview": "python3 -m http.server 8080 --directory dist",
    "clean": "rm -rf dist",
    "rebuild": "npm run clean && npm run build"
  }
}
